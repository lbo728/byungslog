---
import { supabase } from "../lib/supabase";

import WrieteLayout from "../layout/WriteLayout.astro";
import LoginModal from "../component/LoginModal.astro";
// import Editor from "../component/Editor.tsx";

import "../style/markdown.css";
import PostWriteEditor from "@component/PostWriteEditor.astro";

const {
  data: { user },
} = await supabase.auth.getUser();

const savedTitle = "";
const savedContent = "";
const savedTags: string[] = [];
---

<WrieteLayout title="Write Post">
  <!-- <LoginModal isVisible={!user} /> -->
  <PostWriteEditor
    savedTitle={savedTitle}
    savedContent={savedContent}
    savedTags={savedTags}
  />
</WrieteLayout>
